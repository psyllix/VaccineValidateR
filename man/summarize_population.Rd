% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_population.R
\name{summarize_population}
\alias{summarize_population}
\title{Summarize Population-Level Immunization Metrics}
\usage{
summarize_population(
  patients,
  visits,
  years = NULL,
  metrics = NULL,
  id_col = "STUDY_ID",
  dob_col = "DOB",
  verbose = TRUE,
  output_format = c("long", "wide")
)
}
\arguments{
\item{patients}{A \code{data.table} of patient-level records that has already
been processed by \code{evaluate_population()} (contains metric columns and
attached attribute \code{population_metric_definitions}).}

\item{visits}{A \code{data.table} of patient visits including at least
\code{STUDY_ID} and \code{VISIT_DATE}.}

\item{years}{A numeric vector of evaluation years to include in the summary.}

\item{metrics}{Character vector of metric names (e.g.,
\code{"HEDIS_ADOL2"}, \code{"UTD_CIS10"}). These must exist as columns in
\code{patients}. If not specified, will return all possible metrics on the patient table.}

\item{id_col}{Column name in \code{patients} identifying unique patients.
Default is \code{"STUDY_ID"}.}

\item{dob_col}{Column name in \code{patients} containing date of birth.
Default is \code{"DOB"}.}

\item{verbose}{Logical; if \code{TRUE}, progress messages are printed.
Default is \code{TRUE}.}

\item{output_format}{Either \code{"long"} (default; stacked rows with metric
labels) or \code{"wide"} (one row per year, metrics as columns).}
}
\value{
A \code{data.table} summarizing the requested metrics:
\itemize{
\item In "long" format: one row per metric-year with counts, percents,
and metric labels.
\item In "wide" format: one row per evaluation year, columns for each
metric.
}
}
\description{
This function summarizes patient-level population immunization metrics
(created by \code{population_metrics()}) across evaluation years, optionally
returning results in long or wide format. Metrics are grouped by evaluation
year (e.g., age 2 for CIS, age 13 for ADOL).
}
\details{
Evaluation age is inferred from the metric name:
\itemize{
\item Metrics containing \code{"CIS"} → evaluated at age 2 (Childhood)
\item Metrics containing \code{"ADOL"} → evaluated at age 13 (Adolescent)
\item All others → evaluated at age 18 (Default/young adult)
}
}
