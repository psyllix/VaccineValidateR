% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_cohort.R
\name{create_cohort}
\alias{create_cohort}
\title{Generate Cohort List with Age Calculations}
\usage{
create_cohort(
  patients,
  date_of_birth_column_name = "DOB",
  study_id_column_name = "STUDY_ID",
  reference_date = Sys.Date(),
  verbose = TRUE
)
}
\arguments{
\item{patients}{A \code{data.table} containing at least patient identifiers and date of birth.}

\item{date_of_birth_column_name}{String. Name of the DOB column in \code{patients}. Default is \code{"DOB"}.}

\item{study_id_column_name}{String. Name of the patient identifier column. Default is \code{"STUDY_ID"}.}

\item{reference_date}{Optional. A \code{Date} used for age calculation. Default is \code{Sys.Date()}.}

\item{verbose}{Logical. Whether to print progress messages. Default is \code{TRUE}.}
}
\value{
A \code{data.table} with one row per patient, including:
  \itemize{
    \item \code{DOB}: earliest date of birth (as Date)
    \item \code{AGE_DAYS}: age in days at \code{reference_date}
    \item \code{AGE_YEARS}: age in years (decimal, using YEAR_LENGTH)
  }
}
\description{
This function generates a cohort list from a dataset containing patient identifiers and 
dates of birth. It validates required columns, optionally renames them, ensures date consistency, 
and calculates age in both days and years.
}
\examples{
library(data.table)

# Example patient data
dt <- data.table(
  STUDY_ID = c(1, 2, 3),
  DOB = c("2020-01-15", "2019/07/30", "15-Mar-2018")
)

# Create cohort with default reference date (today)
cohort <- create_cohort(dt)
print(cohort)

# Specify a fixed reference date for reproducibility
cohort_fixed <- create_cohort(dt, reference_date = as.Date("2025-01-01"))
print(cohort_fixed)
}
